{% extends 'base.html.twig' %}

{% block body %}
    <h1>StatusOfProject</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statusOfProject.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statusOfProject.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ statusOfProject.status }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('statusofproject_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('statusofproject_edit', { 'id': statusOfProject.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
